import { LoopNumberPipe } from './loop-number.pipe';

describe('LoopNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new LoopNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
